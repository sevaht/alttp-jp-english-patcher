[build-system]
requires = ["hatchling (>=1.29.0,<2.0.0)", "hatch-vcs (>=0.5.0,<1.0.0)"]
build-backend = "hatchling.build"

[project]
name = "alttp-jp-english-patcher"
dynamic = ["version"]
description = "Graft the US English text/menu subsystems into the A Link to the Past JP 1.0 disassembly."
authors = [
    {name = "Jacob McIntosh", email = "nacitar.sevaht@gmail.com"}
]
readme = "README.md"
license = "Unlicense"
requires-python = ">=3.12"
keywords = ["alttp", "zelda", "snes", "disassembly", "romhack", "translation"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Assemblers",
    "Typing :: Typed",
]
dependencies = [
    "platformdirs>=4.10.0",
]

[project.scripts]
alttp-jp-english-patcher = "alttp_jp_english_patcher.__main__:main"

[dependency-groups]
dev = [
    "black (>=26.3.1,<27.0.0)",
    "build (>=1.4.2,<2.0.0)",
    "mypy (>=1.20.0,<2.0.0)",
    "pytest (>=9.0.2,<10.0.0)",
    "ruff (>=0.15.8,<0.16.0)",
    "twine (>=6.2.0,<7.0.0)",
    "vulture (>=2.14,<3.0.0)",
]

[project.urls]
Homepage = "https://github.com/sevaht/alttp-jp-english-patcher"
Source = "https://github.com/sevaht/alttp-jp-english-patcher"
Issues = "https://github.com/sevaht/alttp-jp-english-patcher/issues"

[tool.hatch.version]
source = "vcs"
fallback-version = "0.0.0.dev0"

[tool.hatch.version.raw-options]
fallback_version = "0.0.0"

[tool.hatch.build.targets.wheel]
packages = ["src/alttp_jp_english_patcher"]

[tool.hatch.build.targets.sdist]
include = ["/src", "/tests", "/LICENSE"]

[tool.black]
line-length = 79
skip-magic-trailing-comma = true
# deploy/ holds the target-side scripts (their own old style); tools/ holds
# standalone dev/test scripts (own old style too, predates this package).
# Leave both alone.
force-exclude = "(src/alttp_jp_english_patcher/deploy|tools)/.*"

[tool.mypy]
strict = true
exclude = ["^src/alttp_jp_english_patcher/deploy/", "^tools/", "^dist/"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# `slow` tests (e.g. the full-build base-hash drift guard) are opt-in: run them
# with `pytest -m slow`; the default run (and ./checks) deselects them.
markers = ["slow: long-running; run with `pytest -m slow`"]
addopts = "-m 'not slow'"

[tool.ruff]
line-length = 79
extend-exclude = ["src/alttp_jp_english_patcher/deploy", "tools"]

[tool.ruff.lint]
extend-ignore = ["COM812", "COM819", "E203", "TD003"]
select = [
    "A", "ANN", "ARG", "ASYNC", "B", "BLE", "C4", "C90", "COM",
    "DTZ", "E", "EM", "ERA", "F", "FA", "FLY", "FURB", "I",
    "ISC", "LOG", "N", "PERF", "PGH", "PIE", "PLR", "PLW", "PTH",
    "Q", "RET", "RSE", "RUF", "S", "SIM", "TC", "TCH", "TD",
    "TID", "TRY", "UP",
]

# generate.py is intentionally a set of big, self-contained subsystem
# generators (each subsystem's constants + helpers + build inline in one
# function), so the per-function complexity/statement caps do not apply.
[tool.ruff.lint.per-file-ignores]
"src/alttp_jp_english_patcher/generate.py" = ["C901", "PLR0915"]
"checks" = ["S603"]
"tests/**/*" = ["PLR2004", "S101"]

# The vendored snes_assembly_parser exports a broad public API this package only
# partly uses, so vulture (only) skips it -- it has its own dead-code gate
# upstream. It is still formatted/linted/type-checked like the rest of the repo.
[tool.vulture]
min_confidence = 70
exclude = [
    "src/alttp_jp_english_patcher/deploy/",
    "src/alttp_jp_english_patcher/snes_assembly_parser/",
]
